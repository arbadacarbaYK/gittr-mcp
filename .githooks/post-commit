#!/usr/bin/env bash
# After each commit: if package.json version bumped, publish MCPB to GitHub (when token available).
ROOT="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
if [[ -f "$ROOT/scripts/ensure-github-release.sh" ]]; then
  bash "$ROOT/scripts/ensure-github-release.sh" || true
fi
