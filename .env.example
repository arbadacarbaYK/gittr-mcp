# Copy to .env and add real values. Never commit .env (add to .gitignore).
# Migrated identity credentials go here so the agent can fulfill tasks (Clawstr, X, Lightning, etc.).

# --- Nostr (Clawstr, zaps, DMs) ---
# NOSTR_NSEC=nsec1...
# NOSTR_NPUB=npub1...

# --- Lightning ---
# SPARK_WALLET_MNEMONIC=... (restore to ~/.spark-wallet.json if using Spark)
# LIGHTNING_ADDRESS=...@btip.nl (or own address; bot should create own for sending)

# --- Clawsino ---
# CLAWSINO_API_KEY=...

# --- Moltbook (ZapOps) ---
# MOLTBOOK_AGENT_ID=...
# MOLTBOOK_API_KEY=moltbook_sk_...
# MOLTBOOK_PROFILE_URL=https://moltbook.com/u/ZapOps
# MOLTBOOK_CLAIM_URL=...
# MOLTBOOK_VERIFICATION_CODE=...

# --- Proof of Heat / Yakihonne (NWC) ---
# YAKIHONNE_LIGHTNING_ADDRESS=proofofheat@wallet.yakihonne.com
# NWC_URI=nostr+walletconnect://...

# --- X/Twitter. Bird CLI: ~/.config/bird/config.json5 ---
# BIRD_CONFIG_PATH=/home/arbadacarba/.config/bird/config.json5

# --- OpenRouter (free/paid models: https://openrouter.ai) ---
# OPENROUTER_API_KEY=sk-or-v1-...
# Set this to use OpenRouter as the bot's model (openclaw.json primary can be openrouter/...).

# --- MCP / mcporter (if used) ---
# MCPORTER_...

# --- Email (e.g. IONOS IMAP/SMTP) — for skills that read/send mail ---
# EMAIL_USER=your@example.com
# EMAIL_PASSWORD=...
# EMAIL_IMAP_HOST=imap.ionos.de
# EMAIL_IMAP_PORT=993
# EMAIL_SMTP_HOST=smtp.ionos.de
# EMAIL_SMTP_PORT=587
# (IMAP recommended; use SSL for 993, TLS for 587)
